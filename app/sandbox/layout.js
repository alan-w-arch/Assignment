import "./sandbox.css";

export const metadata = {
  title: "Detection & Resilience Lab",
  description: "Controlled sandbox for job ingestion failure scenarios",
};

export default function SandboxLayout({ children }) {
  return children;
}
